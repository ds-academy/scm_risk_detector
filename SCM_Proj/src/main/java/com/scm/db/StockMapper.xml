<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scm.db.StockMapper">

    <!-- ✅ 특정 회사 코드의 주가 데이터 조회 -->
    <select id="getStockByCompany" parameterType="String" resultType="com.scm.model.StockDTO">
        SELECT COMPANY_CODE, DATE, OPEN, HIGH, LOW, CLOSE, VOLUME
        FROM STOCK_PRICE
        WHERE COMPANY_CODE = #{COMPANY_CODE}
        ORDER BY DATE DESC
    </select>

    <!-- ✅ 특정 날짜의 주가 데이터 조회 -->
    <select id="getStockByDate" parameterType="map" resultType="com.scm.model.StockDTO">
        SELECT COMPANY_CODE, DATE, OPEN, HIGH, LOW, CLOSE, VOLUME
        FROM STOCK_PRICE
        WHERE COMPANY_CODE = #{COMPANY_CODE} AND DATE = #{DATE}
    </select>

</mapper>